alias ~='cd ~'
alias cd..='cd ..'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'

alias ls='ls -G'	# always use colors
alias ll='ls -l'
alias la='ls -A'
alias lal='la -l'
alias l='ls -CF'

# never use vi. only vim.
alias vi='vim'

# This allows me to grep the history without having to write the full command and piping. Fuck, I'm lazy. 
function h() {
    if [ -z "$1" ]
    then
        history
    else
        history | grep "$@"
    fi
}

alias p='pwd'

alias rm-underscore='find . -name '._*' -exec rm -v {} \;'
